import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex:1,
    marginTop: 30,
    flexDirection:'row',
    alignItems:'center',
    borderBottomWidth:1,
    paddingBottom:20
  },

  image: {
    width: 30,
    height: 30,
  },

});

export default styles;
